function past(h, m, s){
  // 자정 이후의 밀리초 단위로 반환하는 함수 작성
  // 시계는 자정 이후 h시간 m분 s초를 표시한다. === 0시간 0분 1초 
  // 시간을 밀리초 단위로 반환 === h를 1000초 단위로 변환
  // 대신 자정 이후의 시간
  // 61000는 === 0시간 1분 1초를 표시한다.
  // 현재시간을 밀리초로 반환해달라
  let y = 1000*s;
  let j = 60000*m;
  let k = 3600000*h;
  
 return y + j + k;
}